{{ form_start(form) }}
<div class="container">
    {{ form_widget(form) }}
    <div class="text-center">
        <button class="btn btn-info ">{{ button_label|default('Save') }}</button>
    </div>
</div>
    
{{ form_end(form) }}
